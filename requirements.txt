# AI Proctoring System — core runtime
# Face presence, identity verification, temporal qualification, evidence packaging
# and the live CLI. Behavioural analysis needs requirements-behaviour.txt as well.

opencv-contrib-python>=4.8.0   # YuNet + SFace inference, camera capture, image IO
numpy>=1.24.0
psutil>=5.9.0                  # process memory / CPU sampling for session telemetry
requests>=2.31.0               # model download (scripts/download_models.py)
matplotlib>=3.7.0              # timeline and comparison plots in the offline tools
pytest>=8.0.0
