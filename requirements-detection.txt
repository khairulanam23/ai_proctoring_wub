# Object and wearable detection.
#   yolo11n     — phones, books and other COCO objects (~100 ms/frame CPU)
#   yolov8s-world — open-vocabulary headphones / earbuds / smart watches
#                   (~250 ms per sweep; run on a decimated cadence)
# Neither is required for face presence, identity or evidence packaging.

-r requirements-behaviour.txt
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.15.0
